diff --git a/node_modules/@docusaurus/core/lib/client/docusaurus.js b/node_modules/@docusaurus/core/lib/client/docusaurus.js
index 0f105f5..333b993 100644
--- a/node_modules/@docusaurus/core/lib/client/docusaurus.js
+++ b/node_modules/@docusaurus/core/lib/client/docusaurus.js
@@ -7,9 +7,28 @@
 import { matchRoutes } from 'react-router-config';
 import routesChunkNames from '@generated/routesChunkNames';
 import routes from '@generated/routes';
+import i18n from '@generated/i18n';
 import prefetchHelper from './prefetch';
 import preloadHelper from './preload';
 import flat from './flat';
+
+// Build a set of known locale path segments for prefix stripping
+const localePaths = new Set(
+  i18n.locales.map((l) => i18n.localeConfigs[l]?.path ?? l),
+);
+
+function stripLocalePrefix(pathname) {
+    for (const localePath of localePaths) {
+        const prefix = '/' + localePath;
+        if (pathname === prefix || pathname === prefix + '/') {
+            return '/';
+        }
+        if (pathname.startsWith(prefix + '/')) {
+            return pathname.slice(prefix.length) || '/';
+        }
+    }
+    return pathname;
+}
 const fetched = new Set();
 const loaded = new Set();
 // If user is on slow or constrained connection.
@@ -29,8 +48,10 @@ const prefetch = (routePath) => {
         return false;
     }
     fetched.add(routePath);
+    // Strip locale prefix before matching routes (needed in dev mode)
+    const strippedPath = stripLocalePrefix(routePath);
     // Find all webpack chunk names needed.
-    const matches = matchRoutes(routes, routePath);
+    const matches = matchRoutes(routes, strippedPath);
     const chunkNamesNeeded = matches.flatMap((match) => getChunkNamesToLoad(match.route.path));
     // Prefetch all webpack chunk assets file needed.
     return Promise.all(chunkNamesNeeded.map((chunkName) => {
@@ -52,7 +73,7 @@ const preload = (routePath) => {
         return false;
     }
     loaded.add(routePath);
-    return preloadHelper(routePath);
+    return preloadHelper(stripLocalePrefix(routePath));
 };
 const docusaurus = {
     prefetch,
diff --git a/node_modules/@docusaurus/core/lib/client/normalizeLocation.js b/node_modules/@docusaurus/core/lib/client/normalizeLocation.js
index 350af18..2932775 100644
--- a/node_modules/@docusaurus/core/lib/client/normalizeLocation.js
+++ b/node_modules/@docusaurus/core/lib/client/normalizeLocation.js
@@ -6,6 +6,13 @@
  */
 import { matchRoutes } from 'react-router-config';
 import routes from '@generated/routes';
+import i18n from '@generated/i18n';
+
+// Build a set of known locale path segments for prefix stripping
+const localePaths = new Set(
+  i18n.locales.map((l) => i18n.localeConfigs[l]?.path ?? l),
+);
+
 // Memoize previously normalized pathnames.
 const pathnames = new Map();
 export default function normalizeLocation(location) {
@@ -22,7 +29,21 @@ export default function normalizeLocation(location) {
         pathnames.set(location.pathname, location.pathname);
         return location;
     }
-    const pathname = location.pathname.trim().replace(/(?:\/index)?\.html$/, '') || '/';
+    // Strip locale prefix (e.g. /en/xxx → /xxx) so routes without locale
+    // prefixes (generated in dev mode) can still match locale-prefixed URLs.
+    // This is needed for the locale dropdown to work during development.
+    let pathname = location.pathname.trim().replace(/(?:\/index)?\.html$/, '') || '/';
+    for (const localePath of localePaths) {
+        const prefix = '/' + localePath;
+        if (pathname === prefix || pathname === prefix + '/') {
+            pathname = '/';
+            break;
+        }
+        if (pathname.startsWith(prefix + '/')) {
+            pathname = pathname.slice(prefix.length) || '/';
+            break;
+        }
+    }
     pathnames.set(location.pathname, pathname);
     return {
         ...location,
diff --git a/node_modules/@docusaurus/core/lib/client/preload.js b/node_modules/@docusaurus/core/lib/client/preload.js
index f243cb1..10df5c9 100644
--- a/node_modules/@docusaurus/core/lib/client/preload.js
+++ b/node_modules/@docusaurus/core/lib/client/preload.js
@@ -5,7 +5,27 @@
  * LICENSE file in the root directory of this source tree.
  */
 import routes from '@generated/routes';
+import i18n from '@generated/i18n';
 import { matchRoutes } from 'react-router-config';
+
+// Build a set of known locale path segments for prefix stripping
+const localePaths = new Set(
+  i18n.locales.map((l) => i18n.localeConfigs[l]?.path ?? l),
+);
+
+function stripLocalePrefix(pathname) {
+    for (const localePath of localePaths) {
+        const prefix = '/' + localePath;
+        if (pathname === prefix || pathname === prefix + '/') {
+            return '/';
+        }
+        if (pathname.startsWith(prefix + '/')) {
+            return pathname.slice(prefix.length) || '/';
+        }
+    }
+    return pathname;
+}
+
 /**
  * Helper function to make sure all async components for that particular route
  * is preloaded before rendering. This is especially useful to avoid loading
@@ -15,7 +35,8 @@ import { matchRoutes } from 'react-router-config';
  * @returns Promise object represents whether pathname has been preloaded
  */
 export default function preload(pathname) {
-    const matches = Array.from(new Set([pathname, decodeURI(pathname)]))
+    const strippedPathname = stripLocalePrefix(pathname);
+    const matches = Array.from(new Set([strippedPathname, decodeURI(strippedPathname)]))
         .map((p) => matchRoutes(routes, p))
         .flat();
     return Promise.all(matches.map((match) => match.route.component.preload?.()));
